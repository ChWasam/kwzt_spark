apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: kafka-consumer
  labels:
    app: kafka-consumer
spec:
  output:
    to:
      kind: ImageStreamTag
      name: kafka-consumer:latest
  source:
    type: Git
    git:
      uri: https://github.com/ChWasam/spark_eda_k8s.git # change accordingly
      ref: main
    contextDir: consumer
    sourceSecret:
      name: mygithub-secret # Replace with your actual secret name if needed
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  triggers:
  - type: ConfigChange
  - type: ImageChange
